# ============================================
# 生产环境配置模板
# 复制此文件为 .env 并修改配置值
# Linux/Mac: cp .env.example .env
# Windows:   Copy-Item .env.example .env
#
# ⚠️ 警告：生产环境必须配置以下必填项！
# 默认值仅用于开发环境，存在安全风险！
# ============================================

# ---------- 必填配置（生产环境必须修改） ----------

# 数据库密码（必填，请使用强密码）
# 默认 dev_password_123 仅用于开发，生产环境必须更换！
MYSQL_ROOT_PASSWORD=your_secure_password_here

# JWT签名密钥（必填，至少32字符的随机字符串）
# 默认密钥仅用于开发，生产环境必须更换！
JWT_SECRET=your-very-long-and-secure-jwt-secret-key-at-least-32-chars

# CORS允许的来源（生产环境必须配置具体域名）
# 默认 * 仅用于开发，生产环境必须限定域名！
CORS_ALLOWED_ORIGINS=https://your-domain.com

# ---------- 可选配置 ----------

# 数据库名称
MYSQL_DATABASE=elderly_care

# 端口配置
MYSQL_PORT=3306
BACKEND_PORT=8080
FRONTEND_PORT=8081

# Spring环境（生产环境建议设为 prod）
SPRING_PROFILES_ACTIVE=prod

# 日志级别（生产环境建议 info 或 warn）
LOG_LEVEL=info

# MyBatis日志（生产环境建议关闭SQL日志）
MYBATIS_LOG_IMPL=org.apache.ibatis.logging.nologging.NoLoggingImpl
